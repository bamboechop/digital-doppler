import { db, notes, users } from 'astro:db';

// https://astro.build/db/seed
export default async function seed() {
  // await db.insert(users).values([
  //   { user_id: 37084588, user_name: 'bamboechop', display_name: 'bamboechop' },
  // ]);
  // await db.insert(notes).values([
  //   { user_id: 37084588, text: 'Hello, world!', date: new Date(), status: 'completed' },
  // ]);
}
